# Vehimap i18n and formatting

Vehimap 2.0 is entering a localization-first phase before larger new features and before Android UI work. The first supported languages are English and Czech. English is the neutral fallback language and Czech remains the primary development and QA language.

## Source of UI text

- User-facing UI strings belong in `Vehimap.Application/Resources/Strings.resx` and `Strings.cs-CZ.resx`.
- `Strings.resx` is the English fallback.
- `Strings.cs-CZ.resx` contains the Czech translation and is used for `cs-CZ`.
- Every new resource key must be added to both files in the same change.
- Resource keys use dotted, stable names such as `Settings.Title`, `VehicleEditor.Name` or `MainMenu.App.Settings`.
- Keep keys semantic. Do not encode the exact English wording into the key.
- Avoid reusing one key for different contexts if grammar or screen-reader wording may diverge later.

## Translator workflow

Translators can edit the `.resx` files directly, but a dedicated editor is strongly preferred. A practical option is [Local ResX Editor](https://apps.microsoft.com/detail/9pkht426fq1k), which can edit `.resx` resources without requiring Visual Studio or manual XML edits.

When handing files to translators:

- Give them both `Strings.resx` and `Strings.cs-CZ.resx` so they can compare source and target text.
- Ask them to preserve placeholders such as `{0}`, `{1}` and `{2}` exactly.
- Ask them not to translate resource keys.
- Ask them to keep accelerator hints such as `Ctrl+S` and `Escape` consistent with the actual UI.

## Settings

The first i18n settings are intentionally small and safe:

- language: `system`, `cs-CZ`, `en-US`
- thousands separator: language default, space, comma, dot, none
- decimal separator: language default, comma, dot
- distance unit: kilometers, miles
- fuel volume unit: liters, US gallons, imperial gallons
- currency: CZK, USD, EUR, GBP

Changing language is saved immediately, but the full UI uses the new language after restarting Vehimap. This avoids partial live reload states while the application is still in the 2.0 nightly phase.

Language defaults are only used for missing settings. Existing user choices are never overwritten automatically. The current defaults are:

| Language | Thousands separator | Decimal separator | Distance | Fuel volume | Currency |
| --- | --- | --- | --- | --- | --- |
| Czech (`cs-CZ`) | none | comma | kilometers | liters | CZK |
| English (`en-US`) | comma | dot | miles | US gallons | USD |

Manual language changes in the Settings dialog update only the language choice. They do not reset already selected separators, units or currency.

## Installer language handoff

The Windows Inno Setup installer offers English and Czech. After installation it writes a one-shot `installer-preferences.json` file into the channel data folder, for example `%APPDATA%\Vehimap Nightly`.

Vehimap reads this seed during startup after SQLite migration/load:

- if `app/language` is missing, it is set from the installer language
- missing separators, unit settings and currency are filled from the language defaults above
- existing settings stay unchanged
- the seed is removed after successful handling

The installer must not write `settings.ini`. In Vehimap 2.0, `settings.ini` is a legacy migration input only; active settings are stored in `data/vehimap.db`.

## Invariant storage

Storage stays invariant:

- SQLite stores stable internal values.
- Distances remain metric internally unless a future schema migration explicitly says otherwise.
- Fuel volume remains liters internally unless a future schema migration explicitly says otherwise.
- Number and unit preferences affect display, input parsing and exports, not the meaning of existing database values.
- Currency preference affects display and export labels only. It does not convert or reinterpret existing stored monetary values.
- User-facing distance settings, such as the maintenance reminder distance, must display and parse the selected distance unit but persist the normalized kilometer value.
- Editor fields for history odometer, fuel odometer, maintenance intervals, maintenance completion odometer and last-service odometer display the selected distance unit and save normalized kilometers.
- Fuel editor volume displays the selected volume unit and saves normalized liters.
- Switching units in Settings is not a data migration. Existing values are re-rendered through the new unit preference, and saving without semantic changes must not multiply or otherwise shift the stored meaning.
- Switching number separators is also not a data migration. Visible values are re-rendered through the new separator preference while storage stays invariant.
- The resolved thousands separator and decimal separator must not be the same non-empty symbol. Ambiguous combinations such as comma/comma or dot/dot are rejected before parsing user-entered values.

This rule is important for backup compatibility, Android reuse and future imports from older Vehimap builds.

## Formatting rules

- Use `IAppNumberFormatService` for localized number formatting and parsing.
- Use `IAppNumberFormatService.FormatMoney` for localized money formatting together with the selected currency.
- Use `IAppUnitFormatService` for distance and fuel volume display or conversion.
- Do not call `ToString()` on user-visible numbers without an explicit culture or formatting service.
- Keep units visible in UI labels, accessible names or help text when ambiguity is possible.

## Open conformance blockers before public 2.0 testing

The 2.0 nightly can continue iterating with the current incremental localization passes, but the following items are mandatory before broad public 2.0 testing:

- Route all remaining user-visible money through the currency formatting service. Dashboard/cost summaries, fuel analysis, timeline items, global search summaries, service book window/application service/export and cost TSV/HTML reports already use the selected currency. Backup/report messages and any remaining export text where money is shown are still open.
- Decide and document the currency conversion model. Until an exchange-rate source and policy exist, changing the currency preference must not silently reinterpret stored historical amounts.
- Finish unit conformance for every remaining kilometer/liter text outside the already converted editor paths. A localized string that still embeds `km`, `l`, `Kc` or `CZK` directly is only an interim step, not final i18n conformance.
- Verify number separator switching against money, odometer, fuel volume and maintenance thresholds together. Separator preferences are formatting/parsing choices, not data migrations.

Current rule until the broader currency pass lands: storage keeps numeric monetary values invariant and existing legacy meanings unchanged. The Settings dialog stores the currency preference, and the first visible surfaces use it, but a TODO remains wherever a user-visible money output has not yet been routed through `FormatMoney`.

## Accessibility

Localized UI text must still satisfy the accessibility checklist in `ACCESSIBILITY.md`:

- interactive controls need stable `AutomationId`
- accessible names must be human-readable in the active language
- help text should explain examples and keyboard shortcuts when needed
- placeholders are examples, not replacements for accessible names

## Current pilot coverage

The second localization pass covers the most visible shell surfaces:

- Settings, About and Vehicle Editor
- the main menu and application-level menu items
- the vehicle list left panel, filters and data folder labels
- the main workspace tab names and their `Open in window` actions
- selected shell status, About diagnostics labels and About clipboard/status feedback

The next passes extend the pilot to the static surfaces of the core evidence editor dialogs and their workspace views:

- dialog help text, field labels, placeholders, button names and accessibility names come from resources
- unit-aware labels such as odometer, fuel volume and maintenance distance interval are formatted through resource keys with the active unit label
- workspace headings, search/sort controls, list names, action buttons, fuel analysis section headings, attachment actions and list item types come from resources
- the Vehicle Detail workspace uses resources for vehicle action buttons, related-record navigation, list names and list item types while dynamic vehicle summaries stay in view-model/domain localization work for later
- the Service Book window, generated service-book item text and HTML export now use EN/CS resources; user-entered vehicle and record data is still escaped in exported HTML, while trusted resource headings remain readable UTF-8 text
- Global Search and Timeline workspace static surfaces use resources for filters, search fields, sorting, list names, item types, detail headings and keyboard help; Global Search generated result details are now also localized through the domain pass
- Upcoming and Overdue overview workspace static surfaces use resources for search fields, filters, sorting, optional data-issue controls, list names, item types, detail panels and navigation action names; generated filters, summaries, selected-item details, missing-green-card pseudo items and data-issue pseudo items are also localized through the overview domain pass
- Smart Advisor workspace static surfaces use resources for search, priority/category/vehicle filters, action buttons, keyboard help, list names, item type and the detail panel
- Data Audit workspace static surfaces use resources for search, sorting, action buttons, keyboard help, list names and item type
- Costs workspace static and dynamic surfaces use resources for period controls, export actions, primary buttons, search, list names, item type, calculation statuses, period labels, summaries, comparisons, selected-vehicle detail text, search summaries, period validation and export statuses; cost TSV/HTML exports also use resource text, selected currency and selected distance/volume units without changing canonical km/l storage; saved cost-period preferences use stable keys such as `year_to_date` and `custom` while old Czech/English labels remain accepted as compatibility aliases
- Dashboard workspace static surfaces use resources for primary actions, show-on-launch control, keyboard help, section headings, audit/cost/timeline list names and item types; the short "select a service plan first" dashboard status is also localized, while generated summaries and broader workflow statuses remain in the later dynamic/domain localization pass
- Maintenance and Vehicle Detail workspace code-behind now uses resources for short action statuses such as missing recommended templates, selecting a service plan first and an empty vehicle bundle
- the Vehicle Starter Bundle dialog uses resources for window chrome, list/detail controls, selection buttons, summaries and accessible item labels; starter template catalog values remain a separate template-id/catalog pass because confirmed templates become persisted user data
- Reminder and Record editor runtime statuses now use resources for create/edit prompts, validation errors, save/delete/advance outcomes, attachment availability text, file picker titles and attachment actions such as opening the file/folder or copying the resolved path
- the core evidence workspaces History, Fuel, Reminders, Maintenance and Records now use resources for runtime empty states, search result summaries, selected-item detail panels and screen-reader item labels; stable internal values such as attachment mode identifiers remain a separate mapping pass
- the maintenance completion dialog uses resources for title, help text, field labels, unit-aware odometer wording, validation errors and save/cancel names
- update check and update download progress dialogs use resources for titles, help text, action buttons, clipboard statuses, progress text and generated update details
- the platform update service uses resources for check results, manifest/network/download/hash/size validation, automatic-install availability reasons and installer/archive preparation progress; App build info also localizes the runtime mode shown in About diagnostics
- the short desktop notification window uses resources for title, accessible window name, help text and heading; generated notification messages remain in the dynamic/domain pass
- the first dynamic/domain localization pass covers fuel analysis: generated availability statuses, warnings, group fallbacks, projected summary lines, severity labels and screen-reader item labels use resource keys
- the second dynamic/domain localization pass covers the audit engine: generated audit categories, titles, messages, unknown-vehicle fallback, severity labels and audit summaries use resource keys; Smart Advisor accepts both Czech and English audit categories while broader recommendation text remains in a later pass
- the third dynamic/domain localization pass covers Smart Advisor: generated status text, priority and category labels, action labels, audit/fuel/cost recommendation wrappers and cost-per-kilometer recommendations use resource keys
- the fourth dynamic/domain localization pass covers Timeline: generated item kind labels, technical/green-card/document titles, deadline statuses, maintenance distance statuses, fallback values, liters, odometer text and basic cost text use resource keys; Smart Advisor recognizes both Czech and English timeline statuses
- the fifth dynamic/domain localization pass covers Global Search: visible section labels, generated summaries, fallback values, attachment modes and selected-result detail text use resource keys while internal navigation entity identifiers remain stable and are not translated
- the sixth dynamic/domain localization pass covers Upcoming/Overdue overviews and the Dashboard timeline: generated filter labels, empty states, item details, overview summaries, missing-green-card pseudo items and data-issue pseudo items use resource keys while old Czech/English saved filter labels remain accepted as compatibility aliases
- the seventh dynamic/domain localization pass covers Quick Actions and short workspace status text: nearest technical/green-card/reminder/maintenance/document actions, filtered review actions, missing-green-card review flow, current background notification opening, workspace refresh statuses, Smart Advisor navigation statuses and fuel-analysis selection statuses use resource keys; Quick Actions and background snapshots recognize both `Bez upozornění` and `No alert` as non-actionable timeline statuses
- the eighth app-shell workflow pass covers application-level statuses and dialogs for backup export/import, vehicle packages, printable reports, SQLite health checks, data and migration folders, external links, update failures, the app-shell controller and background/tray snapshots
- the ninth service-book pass covers the Service Book window, generated entries, navigation statuses and HTML export through EN/CS resources
- the tenth calendar/timeline pass covers manual ICS export summaries, ICS descriptions, export status messages, timeline summaries and selected timeline details through EN/CS resources; timeline filter preferences are stored as stable `all`, `future` and `past` values while old Czech/English labels remain accepted as compatibility aliases
- global safety dialogs such as confirmation prompts and the SQLite data set health check use resources for titles, help text, button names and copy/status feedback
- standalone workspace window chrome uses resources for accessible window names, help text and close buttons; the hosted workspace content remains covered by its own pass
- the accessible `Tray actions` window uses resources for its title, description, section headings, button names, help text and view-model defaults; the native tray menu remains a short convenience menu and is not the screen-reader-first path
- global ComboBox keyboard help, pending-edit lock/confirmation messages, the localized target action names used inside those confirmations and the GitHub feedback issue template use resources so application-level shell text can follow the active language
- installer locale seed status messages, backup restore confirmation, the short native tray menu and close-action prompts for standalone evidence windows use resources as part of the app-level shell edge localization pass
- SQLite 2.0 migration and health diagnostics use resources for automatic migration/cleanup messages, `PRAGMA quick_check`, schema marker, attachment-folder details and the text copied from the Data Store Health dialog
- remaining dynamic workflow status messages and domain-generated analysis text outside fuel analysis, the audit engine, Smart Advisor wrappers, Timeline, Global Search, due-date overview/dashboard timeline, Quick Actions, app-shell workflow passes, the Reminder/Record editor runtime pass and the core evidence workspace runtime pass remain intentionally out of this pass

These areas are guarded against new Czech hardcoded UI text. Remaining broader workflow status messages, generated summaries and domain analysis text are intentionally left for later passes so each step stays reviewable.

## Development rules

- Commit messages are always English.
- New UI text in the pilot localization areas must be added via resources, not hardcoded directly in XAML or C#.
- The current pilot areas are Settings, About including the localized runtime mode, Vehicle Editor, the main shell surfaces listed above, History/Fuel/Maintenance/Reminder/Record editor dialogs, Reminder/Record runtime editor statuses and attachment actions, core evidence workspace runtime summaries/details/search summaries/item labels, the Maintenance Completion dialog, the Vehicle Starter Bundle dialog, update check/progress dialogs and platform update-service results, the short desktop notification window, the fuel analysis domain pass, the audit engine domain pass, the Smart Advisor domain pass, the Timeline domain pass, the Global Search domain pass, the due-date overview/dashboard timeline domain pass, the Quick Actions/domain status pass, app-shell workflow statuses, the Service Book pass, the manual ICS export pass, the Vehicle Detail, Global Search, Timeline, Upcoming Overview, Overdue Overview, Smart Advisor, Data Audit, Costs and Dashboard workspaces, the static History/Fuel/Maintenance/Reminder/Record workspace surfaces, short Dashboard/Maintenance/Vehicle Detail action statuses, Confirmation, Data Store Health, standalone workspace window chrome, the accessible Tray Actions window, global ComboBox help, pending-edit protection text and action descriptions, GitHub feedback issue templates, installer locale seed statuses, backup restore confirmation, SQLite migration/health diagnostics, the short native tray menu and standalone evidence-window close-action prompts.
- Unit tests use an explicit Czech default culture so CI runners do not silently change historical expectations. Tests that verify English output must opt in with their own localizer or culture setup.
- Internal identifiers that drive navigation, storage or compatibility are not resource keys. Localize only the visible label around them, and keep tests for any intentionally stable identifier.
- Internal technical documentation such as `ACCESSIBILITY.md` may remain English.
